# Pending vector

A pending marker is lint-clean; only --release fails on it.

<!-- pending live-run: needs n0 discovery reachable -->
```console
$ swoosh ping bf01hcq6… --transport iroh
rtt 24 ms
```
