# Coverage vector

The loose scan counts this marker; the strict parser refuses it (no space after the colon), so
the coverage equation fails instead of the page passing quietly.

<!-- capture:swoosh status -->
```console
$ swoosh status
direct to ed01hcq6…
```
